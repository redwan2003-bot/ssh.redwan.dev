{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "buildCommand": "cargo build --release",
    "startCommand": "/usr/local/bin/ssh-portfolio",
    "restartPolicyType": "ON_FAILURE"
  }
}