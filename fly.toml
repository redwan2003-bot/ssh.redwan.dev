# See https://fly.io/docs/reference/configuration/ for information about how to use this file.

app = "ssh-redwan-dev"
primary_region = "cdg"

[build]

[env]
  PORT = "2222"

# SSH is a raw TCP service, not HTTP
[[services]]
  internal_port = 2222
  protocol = "tcp"

  [[services.ports]]
    port = 22
